---
# METHOD OVERVIEW — the framing document for an entire therapy method.
# Write exactly one of these per method.
id: REPLACE_method_overview        # e.g. cbt_overview
title: REPLACE                     # e.g. "CBT — overview"
type: method_overview
method: REPLACE                    # CBT | DBT | ACT | schema
domain: psychoeducation
audience: clinician
evidence_level: REPLACE
language: en
links_patterns: []                 # patterns this method is well-suited to
links_hypotheses: []               # hypotheses this method commonly addresses
sources:
  - "REPLACE primary treatment manual. Year."
tags: []
---

<!--
WHAT TO WRITE: the orienting document the agent and reviewer use to understand how
this method works and when to lean on it. 400–800 words (max 1200).
-->

## Core idea
REPLACE: the central mechanism of change in 2–4 sentences.

## Key concepts / building blocks
REPLACE: the handful of concepts the method rests on (bullet list with one-line glosses).

## How a course of work is typically structured
REPLACE: stages/arc, and what early vs later work looks like.

## Best-fit indications
REPLACE: which patterns/hypotheses/domains this method serves well (use taxonomy terms).

## Cautions and contraindications
REPLACE: where this method is a poor fit or needs adaptation/supervision.

## Stance the agent should take
REPLACE: the tone/posture that fits this method (e.g. DBT = validation-first;
ACT = open, accepting; CBT = collaborative empiricism).

## Documents that belong to this method
REPLACE: list the interventions, psychoeducation pieces, and homework ids that make
up this method's set (keep updated as you author them).
