---
# INTERVENTION PROTOCOL — a single technique, described so the agent can guide it.
id: REPLACE_snake_case_unique_id
title: REPLACE short title
type: intervention_protocol
method: REPLACE
domain: REPLACE
audience: clinician        # guidance for how the agent runs the technique
evidence_level: REPLACE
language: en
links_patterns: []         # patterns this technique addresses
links_hypotheses: []
session_modes: []          # when it fits: exploration | support | psychoeducation | ...
requires_supervision: false   # true for exposure/trauma/chair-work etc.
sources:
  - "REPLACE Author. Manual/Article. Year."
tags: []
---

<!--
WHAT TO WRITE: one technique, step by step, so the agent can walk a user through it
conversationally. 200–500 words (max 700). One technique only.
-->

## Purpose
REPLACE: what this technique is for, in 1–2 sentences.

## When to use it
REPLACE: indications and the session modes/patterns where it fits.

## When NOT to use it / cautions
REPLACE: contraindications. If it needs a human therapist present, say so and set
`requires_supervision: true`.

## Steps
1. REPLACE
2. REPLACE
3. REPLACE
(Keep steps small and conversational — what the agent says/asks at each step.)

## Example prompts the agent can use
- "REPLACE a sample question or reflection"
- "REPLACE another"

## How to tell it's working / next step
REPLACE: signs of progress, and what to do next or assign as homework (link a
homework `id` if relevant).
